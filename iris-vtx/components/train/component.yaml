name: train

inputs:
  - {name: training_data, type: CSV}

outputs:
  - {name: model, type: Model}

implementation:
  container:
    image: ...
    command: ["python", "train.py"]
    args:
      - --data-path
      - {inputPath: training_data}
      - --model-dir
      - {outputPath: model}